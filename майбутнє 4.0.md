# Стратегічний план: EDwIC Майбутнє 4.0 🚀

Цей документ описує архітектурні зміни для наступної великої версії ПЗ, які дозволять проекту стати модульною платформою з підтримкою різного заліза.

## 1. Концепція "Залізного Паспорта"
Для того, щоб пристрій завжди знав свою конфігурацію (навіть після повної перепрошивки або видалення файлів), ми використаємо **EEPROM** (енергонезалежну пам'ять чіпа).

### Структура даних (Passport):
Займає перші 64 байти EEPROM:
*   **Magic Bytes (2 байти)**: `0xED 0x01` — для перевірки, що паспорт існує та валідний.
*   **Display ID (1 байт)**: `0` (64x48), `1` (128x64), `2` (E-Ink - на перспективу).
*   **Sensors Mask (4 байти)**: Бітова маска підключених сенсорів (BME, HTU, DS18 та інші).
*   **Hardware Revision (1 байт)**: Версія вашої плати (заліза).
*   **Checksum (2 байти)**: Для контролю цілісності даних.

## 2. Веб-інтерфейс (Адмін-панель заліза)
Нова прихована або сервісна сторінка `/hardware.html`, де:
1.  Можна обрати тип екрану та датчиків зі списку.
2.  Кнопка **"Записати в EEPROM"** — фіксує конфігурацію "навічно".
3.  Кнопка **"Оновити ПЗ під залізо"** — ініціює Smart OTA.

## 3. Логіка "Смарт-Оновлення" (Smart OTA)
Оскільки GitHub Actions не використовуються, ми впроваджуємо суворий стандарт іменування файлів для релізів:
*   `v4.0.0_OLED64_DS18.bin`
*   `v4.0.0_OLED128_BME280.bin`
*   і так далі.

При запиті оновлення, ESP сама формує ім'я файлу на основі свого **Паспорта** і намагається скачати саме його. Це звільняє користувача від необхідності знати, яку прошивку обирати.

## 4. Організація коду (Для розробника)
Весь код залишається в одному проекті. Використовуються:
*   **Універсальні драйвери**: Код екрану працює через `display.width()` та `display.height()`.
*   **Умовна компіляція (#ifdef)**: Для збірки конкретної версії `v4.0.0_OLED128` у файлі настрою розкоментовується лише одна строчка `#define SCREEN_128x64`.

## 5. Переваги та ризики
*   **Плюс**: Жодних "лівих" прошивок та помилок користувача.
*   **Плюс**: Пам'ять пристрою використовується на 100% ефективно.
*   **Ризик**: Якщо помилитися в налаштуваннях заліза, екран може перестати показувати інформацію.
*   **Заборона**: Скидання паспорта фізичною кнопкою **заборонено** згідно з вимогами безпеки.

---
*Документ створено для версії 4.0. Поточне завдання: Полірування версії 3.x.*
